apiVersion: v1
kind: Secret
metadata:
  name: pod-monitor-vmware-credentials
  namespace: monitoring
  labels:
    app: pod-monitor
type: Opaque
stringData:
  # These are example values - replace with actual values when deploying
  host: "vcenter.example.com"
  username: "vmware-username"
  password: "vmware-password"
